[project]
name = "modelscope-tui"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
    "prompt-toolkit>=3.0.0",
    "rich>=13.0.0",
]

[project.scripts]
modelscope_tui = "src.tui:run"

[tool.setuptools.packages.find]
include = ["src*"]
